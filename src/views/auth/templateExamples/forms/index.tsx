import { Fragment } from "react";
import { useTranslation } from "react-i18next";

const UiExamples = () => {
  const { t } = useTranslation();

  return (
    <Fragment>
      this is to list of ui examples of usage of attached components and such
    </Fragment>
  );
};

export default UiExamples;

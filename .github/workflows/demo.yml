name: 🚀 Deploy and Debug Demo Server
run-name: 🚀 Demo Deploy - ${{ github.actor }} on ${{ github.ref_name }}

concurrency:
  group: ${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    branches:
      - prod
  
jobs:  
  deployment:
    name: Demo Deployment
    runs-on: ubuntu-latest
    environment: demo

    steps:
      - name: Configure SSH
        run: |
          mkdir -p ~/.ssh
          echo "$SSH_KEY" > ~/.ssh/demo.key
          chmod 600 ~/.ssh/demo.key

          cat >>~/.ssh/config <<END
          Host demo
            HostName $SSH_SERVER
            User $SSH_USER
            IdentityFile ~/.ssh/demo.key
            StrictHostKeyChecking no
          END
        env:
          SSH_USER: ${{ secrets.DEMO_SSH_USER }}
          SSH_KEY: ${{ secrets.SSH_KEY }}
          SSH_SERVER: ${{ secrets.DEMO_SSH_SERVER }}

      - name: Deploy to demo
        run: |
          ssh -tt demo << 'EOF'
          set -e
          echo "🔐 Connected to demo machine"

          cd ${{ secrets.DEMO_APP_DIR }}

          echo "📥 Pulling latest code..."
          GIT_SSH_COMMAND="ssh -i ${{ secrets.DEMO_PULL_KEY_PATH }}" git pull

          echo "🐳 Building and restarting fe"

          cd ${{ secrets.DEMO_DOCKER_COMPOSE_DIR }}
          for dir in */; do
            cd ${{ secrets.DEMO_DOCKER_COMPOSE_DIR }}/$dir
            set +x
            docker compose build "fe"
            docker compose up -d "fe"
            set -x
            echo "✅ Deployment complete for $dir"
          done

          set +e
          exit 0
          EOF
